export const consts = {
    CONTEXT_ERROR_MESSAGE: 'In order to use react-hook-popup hooks, you must wrap your application in <PopupProvider>',
    UNFOUND_POPUP: (key: string): string => `Attempted to act on a non-existing popup with key: ${key}`,
};
